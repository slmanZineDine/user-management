export enum Directions {
   RTL = "rtl",
   LTR = "ltr",
}

export enum Languages {
   ENGLISH = "en",
   ARABIC = "ar",
}

export enum Environments {
   PROD = "production",
   DEV = "development",
}
